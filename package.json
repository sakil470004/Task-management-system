{
  "name": "task-management-system",
  "version": "1.0.0",
  "description": "Full-stack task management system with role-based access and audit logging",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "pnpm@8.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "start": "pnpm -r run start"
  },
  "devDependencies": {}
}
